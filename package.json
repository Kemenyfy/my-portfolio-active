{
  "private": true,
  "scripts": {
    // "deploy": "surge ./public --domain kemenyfy.com",
    "start": "browser-sync start --server \"public\" --files \"public\""
  },
  "dependencies": {
    "auth0-js": "^9.8.0",
    "browser-sync": "^2.24.6",
    "stylelint": "^9.4.0",
    "stylelint-config-standard": "^18.2.0",
    "surge": "^0.20.1"
  }
}
